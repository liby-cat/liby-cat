{
  "memory": {
    "name": "memory",
    "connector": "memory"
  },
  "mongo": {
    "host": "${MONGO_HOST}",
    "port": "${MONGO_PORT}",
    "database": "${MONGO_DB}",
    "username": "${MONGO_USER}",
    "password": "${MONGO_PW}",
    "name": "mongo",
    "connector": "mongodb"
  }
}

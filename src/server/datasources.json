{
  "memory": {
    "name": "memory",
    "connector": "memory"
  },
  "mongo": {
    "host": "${MONGO_HOST}",
    "port": "${MONGO_PORT}",
    "database": "${MONGO_DB}",
    "username": "${MONGO_USER}",
    "password": "${MONGO_PW}",
    "name": "mongo",
    "connector": "mongodb"
  },
  "emailDs": {
    "connector": "mail",
    "transports": [
      {
        "type": "smtp",
        "host": "mail.sfaar.com",
        "secure": false,
        "port": 26,
        "tls": {
          "rejectUnauthorized": false
        },
        "auth": {
          "user": "liby@sfaar.com",
          "pass": "GEk{?SE1IgT."
        }
      }
    ]
  }
}

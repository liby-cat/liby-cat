{
  "memory": {
    "name": "memory",
    "connector": "memory"
  },
  "mongo": {
    "host": "${MONGO_HOST}",
    "port": "${MONGO_PORT}",
    "database": "${MONGO_DB}",
    "username": "${MONGO_USER}",
    "password": "${MONGO_PW}",
    "name": "mongo",
    "connector": "mongodb"
  },
  "emailDs": {
    "connector": "mail",
    "transports": [
      {
        "type": "smtp",
        "host": "just13.justhost.com",
        "secure": true,
        "port": 465,
        "tls": {
          "rejectUnauthorized": false
        },
        "auth": {
          "user": "liby@sfaar.com",
          "pass": "StzrdZ!{4)]42@"
        }
      }
    ]
  }
}

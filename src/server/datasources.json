{
  "db": {
    "name": "db",
    "connector": "memory"
  },
  "mongol": {
    "host": "${MONGO_HOST}",
    "port": "${MONGO_PORT}",
    "database": "${MONGO_DB}",
    "username": "${MONGO_USER}",
    "password": "${MONGO_PW}",
    "name": "mongol",
    "connector": "mongodb"
  }
}

'use strict';

angular.module('app')
  .controller('OrgsCtrl', [
    '$scope', '$state', '$stateParams',
    'User', 'Org',
    function($scope, $state, $stateParams,
              User, Org) {
    }
  ]);
